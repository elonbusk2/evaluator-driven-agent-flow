# UI Verification Screenshots

This directory contains screenshots captured during Phase 3 UI/UX verification.

Structure:
```
docs/screenshots/
├── {feature-name}/
│   ├── {page-slug}.png
│   ├── {page-slug}-{action}.png
│   └── login-success.png (if authentication required)
```

Screenshots are referenced in verification reports located in `docs/reports/`.
